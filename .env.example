# Docker Compose configuration. Copy to .env and choose a private password.
POSTGRES_USER=llc_code
POSTGRES_PASSWORD=replace-with-a-random-local-password
POSTGRES_DB=llc_code
POSTGRES_PORT=55432
REDIS_PORT=6379
PISTON_PORT=2000
